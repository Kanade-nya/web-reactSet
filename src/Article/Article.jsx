import {useLocation, useParams} from "react-router-dom";

export default function Article() {
    // const location = useLocation();
    // const params = useParams()
    // const {arts_info} = location.state
    // console.log(params)
    // console.log(state)

    return (
        <>
            <div>

                this is article page
            </div>
        </>
    )
}